# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

delegated: Python 标准库 loopback 服务 + 原生 HTML/CSS/JavaScript 静态前端；运行时不依赖 Node、Obsidian 或云端服务。开发时允许使用浏览器自动化做验收，但发布包应保持零前端运行时依赖。

## Users

主要用户是在 Windows 桌面上长期使用 Clash Verge/mihomo、需要快速判断台湾叶子节点当前是否稳定的个人用户。用户通常在工作或浏览过程中打开监控页，不希望为了看状态启动完整仪表盘、输入 API 密钥或维护复杂服务。

## Product Purpose

节点监控通过 Clash/mihomo 本机 External Controller 定时探测用户配置的叶子节点，把最近一次完整采样、可用率、平均延迟、异常状态和 1/3/7 天真实历史集中到一个独立软件界面中。成功标准是用户打开页面后 10 秒内能判断当前可用节点，30 秒内能定位持续不稳定的节点；刷新期间仍能看到上一轮完整结果。

## Positioning

它不是 Clash 的通用控制面板，也不是带宽测速器：它是一个本机优先、可解释、默认只读的节点质量观察器。SQLite 历史、loopback 快照和安全自动路由策略属于同一运行时；Obsidian 仪表盘只是原项目中的一个可选消费者，不是独立软件的前置条件。

## Operating Context

- 用户在本机运行 Clash Verge/mihomo，并让监控从本地配置自动发现控制器与密钥。
- 服务默认只绑定 `127.0.0.1`，浏览器访问本机页面；监控数据只写入项目目录下的 SQLite。
- 默认监控 `^TW-\d+$` 叶子节点；策略组、URLTest 和 LoadBalance 不作为独立节点采样。
- 用户可查看当日、最近 3 天或 7 天历史，切换节点聚焦和聚合粒度，一键导出当前时间范围的原始 CSV，并在需要时暂停、刷新、配置或关闭服务。

## Capabilities and Constraints

- 已有 Python 核心负责 Clash API、有限并发采样、完整周期提交、SQLite、loopback JSON API、路由判断和可选 Tk 浮点入口。
- 自动路由默认关闭；用户显式开启后，绿色当前节点保持不动，橙色延迟按连续异常次数判断，Timeout/错误在满足安全候选条件时故障转移；写入后必须回读实际节点。
- 延迟探针只说明响应延迟和可达性，不等同于下载速度、带宽或完整业务体验。
- 不模拟 Clash 界面、不读取整个 Vault、不上传数据、不调用 AI 常驻运行、不把密钥写入仓库。
- 导出只生成本机下载文件，包含采样时间、节点、状态、延迟、请求耗时和错误信息，方便交给 AI 分析。
- 公开发布不得包含真实 `monitor_config.json`、SQLite 数据库、缓存或用户密钥。

## Brand Commitments

- 产品名称沿用“节点监控”，不伪装成 Clash 官方组件。
- 界面应直接呈现真实监控状态，状态不能只依赖颜色；“Timeout”“错误”“橙色延迟”等术语保持可解释。
- 独立前端可以替换原仪表盘卡片的视觉世界，但不得改变采样、历史和自动路由的产品事实。

## Evidence on Hand

- 运行时源码：`tw_monitor.py`。
- 需求与验收：`docs/需求沉淀.md`。
- 数据流与 API：`docs/架构说明.md`。
- 现有仪表盘集成合同：原仪表盘项目中的 `13_台湾节点质量卡片.md`、长期体验合同 `WF-12` / `CARD-10` / `DATA-08`。
- 已有后端基线：17 项标准库单元测试和语法检查在独立副本中通过。
- 当前没有可作为独立软件品牌的 logo、客户案例或远程数据；前端不得编造这些内容。

## Product Principles

1. 先判断现在能不能用，再追查为什么。
2. 历史是真实采样的记录，不用装饰性数据填空。
3. 自动路由只有在用户明确授权和规则满足时才改变网络。
4. 本机数据留在本机，服务默认最小暴露面。
5. 软件应能脱离 Obsidian 单独运行，并让第一次使用者知道下一步做什么。

## Accessibility & Inclusion

独立 Web 界面应支持键盘操作、可见焦点、文字状态和 `aria-live` 更新；图表必须提供可读标题、点位明细和非颜色状态线索；交互目标保持触控可用尺寸，并尊重 `prefers-reduced-motion`。

## Open Decisions

- 发布载体（仅源码、Windows 可执行包或安装器）尚未由用户指定；本轮先保证源码目录可直接运行，并为后续打包保留清晰入口。
- GitHub 目标仓库与 `2` 目录尚未从本地 Git 配置中发现；因已找到高度重合项目，本轮不自行创建或上传远程目标。
