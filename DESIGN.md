# 独立节点监控 Web 设计合同

<!-- design-contract: standalone-monitor-v1 -->

## 方向

“夜间铁路调度板”：页面像一块克制的本地调度台，先回答“现在有多少节点可用、Clash 实际走哪条线、最近有没有失速”，再展开历史和设置。它不是通用 SaaS 仪表盘，也不是把 Obsidian 卡片放大后的复制品。

## 视觉语言

- 画布使用深石墨和炭黑层级，边界使用细线，不用玻璃、渐变、发光装饰或远程图片。
- 琥珀色只表示操作、时间轴和路由控制；绿色、橙色、红色只表示健康状态，避免颜色语义漂移。
- 延迟、时间、节点名和计数使用等宽数字或等宽字体，保证扫描和对比稳定。
- 第一视图顺序固定为：当前健康度 → 实际出站 → 连续趋势 → 节点明细 → 设置。异常信息与数据点相邻，不藏在装饰卡片里。
- 图表只绘制 API 返回的真实采样；没有采样时显示空状态，不生成演示数据。

## 交互合同

- 默认只读。自动路由关闭时，页面明确写出“只监控和记录”。
- 刷新、暂停、保存设置和停止服务都必须显示进行中、成功或失败反馈；采样中的手动刷新不会制造第二轮采样。
- 当前节点必须来自 Clash 实际回读，而不是上一轮计划目标；路由切换后显示实际结果。
- 图表支持 1/3/7 天、聚合粒度、节点聚焦、缩放、拖拽、键盘平移和点位提示；断点显示为 Timeout/失败点。
- 趋势区提供一个明确的“导出 CSV”次级操作；它导出当前 1/3/7 天范围的原始采样，不把展示聚合误当成数据丢失。
- 离线时保留页面结构和解释性空状态，不伪造在线数值；重新连接可以恢复。

## 响应式与可访问性

- 1180px 以下压缩图表控制；880px 以下将指标和路由信息重排；620px 以下变为单列，表格保留横向滚动。
- 所有操作控件使用原生按钮、输入框、选择框和可见焦点；图表提供 `role="img"`、键盘焦点和文本提示。
- 颜色不是唯一状态通道：节点同时显示文字，健康轨同时提供比例文字，错误同时提供说明。
- `prefers-reduced-motion` 下关闭非必要动效；页面不依赖外部字体或网络资源。

## 参考画面

视觉参考保存在 `docs/视觉参考/节点监控-参考图-v1.png`，它只用于校准空间、密度、色彩和首屏叙事；运行页面永远以实时 API 数据为准。
