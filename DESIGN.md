# Clash Node Monitor Web 设计合同

<!-- design-contract: standalone-monitor-v2 -->

## 方向

“夜间铁路调度板”：页面像一块克制的本地调度台，先回答“现在有多少节点可用、实际连接是否正常、最近有没有失速”，再展开历史和设置。它是可独立分发的软件界面，不依赖其它产品外壳。

## 视觉语言

- 深色主题使用深石墨、炭黑和细边界；浅色主题使用暖白、雾灰和同一套琥珀强调色。
- 琥珀色只表示操作、时间轴和路由控制；绿色、橙色、红色只表示健康状态，避免颜色语义漂移。
- 延迟、时间、节点名和计数使用等宽数字或等宽字体，保证扫描和对比稳定。
- 第一视图顺序固定为：连接状态 → 当前健康度 → 趋势 → 节点明细 → 设置。
- 图表只绘制真实采样；没有采样时显示空状态，不生成演示数据。

## 连接引导

- 启动后自动显示控制器发现状态，不要求用户先编辑 JSON。
- 自动发现失败时，连接卡片提供“连接高级设置”和“测试连接”，将地址、密钥和节点数量反馈放在同一上下文中。
- 密钥不会回显到状态接口；页面只显示“已保存/未设置”这种非敏感状态。
- 默认告诉用户软件是本机运行、不会上传数据，避免把连接问题误认为网页故障。

## 交互合同

- 默认只读。自动路由关闭时，页面明确写出“只监控和记录”。
- 刷新、暂停、保存设置、测试连接、导出和停止服务都显示进行中、成功或失败反馈。
- 当前节点必须来自控制器实际回读，而不是上一轮计划目标。
- 图表支持 1/3/7 天、展示聚合、节点聚焦、缩放、拖拽、键盘平移和点位提示；断点显示为 Timeout/失败点。
- 趋势区提供明确的“一键导出 CSV”操作，导出当前时间范围的原始采样，不把展示聚合误当成数据丢失。
- 离线时保留页面结构和解释性空状态，不伪造在线数值；重新连接可以恢复。

## 响应式与可访问性

- 1180px 以下压缩图表控制；880px 以下重排指标和路由信息；620px 以下变为单列，表格保留横向滚动。
- 所有操作控件使用原生按钮、输入框、选择框和可见焦点；图表提供 `role="img"`、键盘焦点和文本提示。
- 颜色不是唯一状态通道：节点同时显示文字，健康轨提供比例文字，错误提供说明。
- `prefers-reduced-motion` 下关闭非必要动效；页面不依赖外部字体、图片或网络资源。
