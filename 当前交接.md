# 当前交接

## 项目监控摘要

- 项目状态：completed
- 当前阶段：核心采样与路由合同已从仪表盘摘出，独立目录新增同源 Web 控制台、视觉合同、无痛启动入口和 AI 分析导出
- 本轮完成：趋势区新增一键导出当前 1/3/7 天范围原始采样的 UTF-8 CSV；保留时间、节点、状态、延迟、请求耗时和错误字段；页面反馈导出行数；原目录和仪表盘工作树均未修改
- 需要用户：否（可直接在本目录运行；独立版已发布到指定 GitHub 仓库）
- 验收或操作入口：在本目录双击 `run_monitor.bat`，或运行 `py tw_monitor.py --server` 后打开 `http://127.0.0.1:17997/`；源码入口为 `https://github.com/devonsagr/2/tree/main`
- 通过后的下一步：按真实 Clash Verge/mihomo 环境继续使用导出的 CSV 做一次 AI 分析验收；不要直接双击 `web/index.html` 作为运行方式
- 当前阻塞：无
- 交付版本：commit `f547752`（已推送 `origin/main`）
- Git 状态：clean，`main` 与 `origin/main` 一致
- 最后更新时间：2026-09-07 Asia/Hong_Kong

## 本轮独立版验证

- 原网络监控目录 `D:\AAAcodex项目\网络监控` 保持原样；独立目录首轮复制排除了真实 `monitor_config.json`、SQLite 数据库和顶层运行缓存。
- 复制后的源文件 SHA-256 与原目录一致；独立目录通过 Python 语法检查和 19 个单元测试。
- `tw_monitor.py --server` 现在同时提供 `/api/*`、`/api/export?days=1|3|7` 和 `web/` 静态资源；新增静态首页与 CSV 下载冒烟测试。
- `web/index.html` 的 CSS/JS 已改为相对路径，直接从仓库子目录或本机服务访问时不会再退回浏览器默认样式；运行方式仍需通过本机 loopback 服务连接 API。
- 页面默认只监控和记录；自动路由仍需用户明确开启，且显示 Clash 实际回读节点。
- 视觉参考位于 `docs/视觉参考/节点监控-参考图-v1.png`；设计方向和组件约束位于 `DESIGN.md`、`docs/视觉方向.md`。

## 既有核心事实（从原项目继承）

- 本机运行 Clash Verge/mihomo，External Controller 为 `127.0.0.1:9097`。
- API 能识别 10 个匹配节点：`TW-1`～`TW-10`；策略组被排除。
- 真实采样已持续运行：当前服务可正常读取本机 Clash Verge/mihomo，当前监控节点为 `TW-6`；最新完整周期 `15577` 为 7 个正常、3 个超时，路由动作 `kept`，当前 `TW-6` 仍为绿色保持。
- `py_compile`、17 个标准库单元测试、节点卡片 9/9 定向测试、插件 TypeScript 检查、统一发布快照生产构建和 Obsidian 安装校验均通过；完整前端套件在并行仪表盘工作树资源竞争下仍有无关超时，未作为本卡片交付依据。
- 自动路由单元测试覆盖：URLTest 子组解析到实际 MATCH Selector、绿色但排名第二保持、橙色连续确认、Timeout/错误首采样立即切换、冷却窗口不拦截故障转移、无绿色候选保持、手动变更优先；主仪表盘桥接与夜间卡片定向测试已通过。
- 统一发布候选/正式 Vault `main.js` SHA-256 均为 `0F291F0C502135EDD7432379588770AA3CFA2C07875960C1AAB0C8156686A398`，`styles.css` 均为 `8E1A8105CED7D120DF73257E8B40CD68527D8222864EE789A363224FC8E21AB6`，`manifest.json` 均为 `6864A5B4FD699334C5417D017C15304D065C91E5D54AA2D21AB075772C5405C3`；安装器校验 46 个产物并保留了目标已有 `data.json`（部署前后 SHA-256=`9F6F31412F7F5DCC1C7F1916BAA3FF0C19B975BEF1C3796A41E7DC5D05316E7F`）。
- 自动实测：当前运行配置中 `TW自动选择` 是 `URLTest`，默认 `MATCH` 实际使用 `主代理`。先将实际出站组临时切到 `TW-7` 验证周期 `15414` 的首个 Timeout 故障转移；部署图表改动后，周期 `15572` 又自发将实际 `主代理` 从 Timeout 的 `TW-10` 切到绿色最低延迟 `TW-6（86 ms）`，并明确记录“故障转移已跳过冷却”，随后周期 `15577` 对绿色 `TW-6` 保持不动；服务 API、`lastRouteEvent` 和 Clash 实际回读均一致。
- 旧数据库中的早期 `route_events` 保留作历史记录，但不再作为当前实际路由的证明；当前卡片优先显示实时控制器回读状态。
- 主仪表盘的本地权威设计合同为 `D:\AAAcodex项目\仪表盘\docs\project\架构\13_台湾节点质量卡片.md`；Obsidian 镜像未声明在本项目内，由主仪表盘项目规则负责。仪表盘本轮节点监控相关提交为 `3d1da92`、`8f2485a`、`81ea5b1`、`da105d4`、`9952144`，统一发布工作树仅作为本次安全构建输入，未被本轮修改。

## 相关文档

- 产品定义：`PRODUCT.md`
- 设计合同：`DESIGN.md`
- 需求保真与分布：`docs/需求沉淀.md`
- 架构和边界：`docs/架构说明.md`
- 视觉方向与参考：`docs/视觉方向.md`、`docs/视觉参考/节点监控-参考图-v1.png`
- 视觉/交互设计基线：`.ui-craft/brief.md`
- 产品进度：`路线图.md`
