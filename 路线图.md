# 路线图

## 当前产品目标

把 Clash/mihomo 节点质量监控做成普通用户可以直接启动的 Windows 本地软件：自动发现本机控制器，展示真实可用性和延迟趋势，并让用户一键导出时间数据给 AI 或其它分析工具。

## 阶段

1. **核心采样闭环**：已完成。实现 External Controller 采样、SQLite 历史、完整周期提交、超时记录和可选自动路由。
2. **独立 Web 控制台**：已完成。提供同源 API、趋势图、节点清单、设置表单、离线状态和本机运行说明。
3. **AI 分析导出**：已完成。趋势区按 1/3/7 天范围一键导出原始 UTF-8 CSV，保留时间、节点、状态、延迟、请求耗时和错误字段。
4. **无痛连接与主题**：已完成。启动后自动打开控制台，自动发现控制器；失败时提供高级连接设置和测试按钮；支持深色/浅色模式。
5. **Windows 分发**：本轮完成。提供 `ClashNodeMonitor.exe`、`run_monitor.bat` 和可重复构建脚本。
6. **后续增强**：待真实用户反馈决定，包括评分、通知、任意日期范围、多探针和安装器。

## 当前风险

- 延迟 API 反映的是配置探针的可达性和响应时间，不是带宽吞吐量。
- 不同 Clash/mihomo 内核或版本可能对节点类型和 delay API 的行为不同，需要以用户本机测试结果为准。
- External Controller 未启用、地址错误或密钥错误时，软件无法替用户修复 Clash 配置，只能提供连接提示。
- EXE 是本地服务加浏览器控制台，不是云端网页；用户需要在自己的 Windows 设备上运行它。
