# 路线图

## 当前产品目标

用本地、轻量、无需 AI 常驻的独立软件观察 Clash Verge 中 TW 叶子节点的可达性和延迟，帮助任何使用者在当前时刻以及 1/3/7 天历史窗口内判断稳定节点。

## 阶段

1. **需求与设计基线**：已完成。目标、P0/P1/P2 分层、边界、数据流和验收标准已写入 `docs/需求沉淀.md`。
2. **首版闭环**：已完成。已实现 API 采样、SQLite、置顶仪表盘卡片、1/3/7 天历史图、无界面模式。
3. **当前环境联调**：已完成。已用本机 Clash Verge 的 9097 控制器完成真实采样，并检查卡片与数据库读写。
4. **界面增强**：已完成。卡片以“最佳节点”作为主趋势，支持 1/3/7 天、单节点切换、默认 30 分钟聚合和手动 1 分钟等细粒度切换、1×～48×（最细 30 分钟视窗）时间缩放；Ctrl+滚轮无需先点击，放大后可拖拽/键盘平移，并修正夜间标题纸签、遮挡与单滚动面。
5. **路由实链路与轻量入口**：已完成。自动路由通过 Clash External Controller 的 PUT 后 GET 回读确认；当前绿色节点保持不动，橙色按确认次数切换，首次 Timeout/错误直接从绿色候选中故障切换且不被旧冷却阻挡；卡片和桌面浮点均显示 Clash 实际当前节点及最近一次延迟，开启自动路由后立即同步当前选择。
6. **独立软件摘取**：已完成。新目录保留原 Python/SQLite 核心，新增同源 `web/` 控制台、浏览器入口、设置表单、实时状态、历史 SVG 趋势和离线空状态；不依赖 Obsidian、Node.js 或第三方 CDN。
7. **AI 分析导出**：已完成。趋势区可按当前 1/3/7 天范围一键导出原始 UTF-8 CSV，保留时间、节点、状态、延迟、请求耗时和错误字段。
8. **后续增强**：待定。告警、评分、任意日期和多探针只有在独立版使用后再决定。

## 当前风险

- 延迟 API 反映的是配置探针的可达性和响应时间，不是带宽吞吐量。
- 不同 Clash 内核/版本可能对 TUIC 节点的 delay API 行为不同，需要以当前 mihomo 实测结果为准。
- 当前工作区没有 Clash Verge 前端源码，因此首版卡片是独立桌面卡片，不嵌入第三方客户端内部。
- 独立版已发布到用户指定的 GitHub 仓库 `https://github.com/devonsagr/2` 的 `main` 分支；仓库只包含源码、文档、测试和配置模板，不包含本机密钥、SQLite 数据或运行缓存。
- GitHub 仓库用于分发源码，不提供远程 Clash 监控服务；使用者仍需在自己的 Windows 机器上运行本地 Python loopback 服务。
